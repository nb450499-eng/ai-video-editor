# Golden Window Samples

These JSON files are schema fixtures for step 0. They are not a real evaluated
dataset yet; human annotated project truth should live under
`projects/<project_id>/truth/<shot_id>/`.
